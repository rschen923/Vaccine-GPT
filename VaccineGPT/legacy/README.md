# Legacy M1/M2 (deprecated)

The historical `src_m1` and `src_m2` implementations are retained only for
lineage and reproducibility. They are not supported training paths and are not
used by the default `train.py` entrypoint. New work must use `vaccinegpt/` and
its L0-L4 contract.
