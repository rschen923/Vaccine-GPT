# UDC records

This directory is reserved for Universal Data Contract (UDC) definitions used by M1 and M2.

Planned entries:
- Sequence metadata
- View-level feature schema
- Label schema
- Model card schema
- Evaluation report schema

The current quick-start implementation keeps the schema minimal but consistent with the versioned lineage protocol.
